#include <bits/stdc++.h>
using namespace std;
 long long fact(int n) {
     long long result = 1;
    for (int i = 2; i <= n; ++i) {
        result *= i;
    }
    return result;
}
 
signed main() {
    int n;
    cin >> n;
    cout << fact(n) << endl;
}
