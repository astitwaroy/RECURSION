int sequence(int n){
    if(n==1) return 1;
    else if(n%2==0) return sequence(n/2)+1;
    else return sequence(3*n+1)+1;
 
}
signed main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout.tie(NULL);
    int n;
    cin>>n;
    cout<<sequence(n);
}
